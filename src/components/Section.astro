---
interface Props {
  number: string;
  label: string;
}

const { number, label } = Astro.props;
---

<div class="flex flex-col leading-2">
  <span class="text-neutral-800 text-[4rem] font-extrabold">{number}</span>
  <span class="font-bold">{label}</span>
</div>
